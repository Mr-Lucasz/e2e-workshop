# Configurações para ambiente corporativo
# Desabilitar verificação SSL (use apenas se necessário)
strict-ssl=false

# Configurar proxy se necessário (descomente e ajuste)
# proxy=http://proxy.corporativo.com:8080
# https-proxy=http://proxy.corporativo.com:8080

# Configurar certificados corporativos
# cafile=./certs/corporate-ca.pem

# Configurações de cache
cache=C:\Users\C919409\AppData\Local\npm-cache
prefer-offline=true

# Configurações do Cypress
cypress-cache-folder=C:\Users\C919409\AppData\Local\Cypress\Cache

# Desabilitar auditoria automática
audit=false
fund=false
